{
  "name": "home-finlytics-v2",
  "version": "1.0.0",
  "description": "",
  "main": "vite.config.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "vite",
    "build": "node -e \"require('fs').cpSync('public','dist',{recursive:true})\" && echo Build complete",
    "deploy": "npm run build && firebase deploy --only hosting"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "firebase": "^12.13.0"
  },
  "devDependencies": {
    "vite": "^8.0.14"
  }
}
